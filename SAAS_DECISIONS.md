# SaaS化 意思決定記録

決定日: 2026-08-28 / 対象: AIレベニュー管理システムのSaaS提供

選択肢の比較検討は `SAAS_ONBOARDING.md` と PR #39 のレビューを参照。
このファイルには**何を決めたか**と**実装上の含意**だけを記録する。決定は再議論しない。

前提の変更: エンタープライズ案件が確定済みのため、
「まずPaaSで走る」という当初案は破棄した（D-03）。

---

## D-01 テナント分離: PostgreSQL RLS を併用する

**決定**: 現在の row-level 分離（全モデルの `tenantId`）に加え、PostgreSQL の
Row Level Security を有効化し、**DBレイヤーでテナント越えを物理的に禁止する**。

**理由**: 会社の異なる複数社へSaaSとして展開するため、アプリ側のバグ1つで
他社データが見える可能性を構造的に排除する。コードレビューによる担保では不十分。

**実装状況**: 実装済み（PR #39）。ローカルPostgreSQLに対する統合テスト9件で
テナント越えの遮断を実測検証している（`pnpm --filter backend test:rls`）。
アプリ層の認可を通過するトークンを用いた検証でも、他社データは404となることを確認済み。

| 構成要素 | 実体 |
|---|---|
| DBポリシー | `prisma/migrations/20260828000000_enable_row_level_security/` （24テーブル） |
| アプリ用ロール作成 | 本番 `prisma/rls-role.sql` / 開発 `docker/initdb/10-app-user.sql` |
| テナントコンテキスト | `src/lib/tenantContext.ts`（AsyncLocalStorage）・`src/lib/prisma.ts`（プロキシと実行ヘルパー） |
| リクエストへの適用 | `src/middlewares/tenantContext.ts`（`/api/v1` 全体に適用） |
| 横断が必要な経路 | ログイン・トークン更新（`authService`）と提供側ADMIN のみ |
| 検証 | `src/lib/rls.integration.test.ts` |

**実装上の必須事項**（順守しないとRLSが無効化される）:
- アプリ用DBロールは **superuser でなく、`BYPASSRLS` も付けない**。
  superuser はRLSを素通りするため、Cloud SQL の既定 `postgres` ユーザーをそのまま使わない
- `tenantId` を持つ全テーブルで `ENABLE ROW LEVEL SECURITY` かつ `FORCE ROW LEVEL SECURITY`
  （FORCE を付けないとテーブル所有者がポリシーを回避する）
- ポリシーは `USING (tenant_id = current_setting('app.tenant_id', true))`
- **テナントIDの設定は必ずトランザクション内で `SET LOCAL` を使う**。
  トランザクション外で `SET` するとコネクションプールの使い回しで
  次のリクエスト（＝別テナント）に設定が残り、他社データが見える。最重要の注意点
- Prisma からは interactive transaction（`$transaction` のコールバック形式）で
  接続を固定し、その中で `SET LOCAL` → クエリの順に実行する
- 提供側 ADMIN（`tenantId` が null）は全テナント横断が必要なため、
  専用の経路（別ロール、または明示的なbypass関数）を用意する。既定は必ず制限側
- テスト: テナントAのトークンで**テナントBのIDを指定して全ルートを叩き**、
  0件または403になることを検証する回帰テストを必ず追加する

## D-02 ユーザー識別: `(tenantId, email)` の複合一意にする

**決定**: `User.email` のグローバル一意をやめ、テナント単位で一意にする。

**理由**: グループ本社・運営受託会社が同一担当者を複数テナントで持つケースに対応するため。
データが増える前の今が最も安価。

**実装状況**: 実装済み（PR #39）。

- 複合一意 `@@unique([tenantId, email])` ＋ 提供側ADMIN用の部分一意インデックス
  （`WHERE "tenantId" IS NULL`。PostgreSQL は NULL 同士を重複と見なさないため、
  これがないと tenantId なしのユーザーが同じメールで何件でも作れてしまう）
- ログインのテナント特定は D-08 のサブドメイン → 明示的な組織コード の順。
  どちらも無く候補が複数ある場合のみ 409 で組織コードを要求する
  （画面はそのときだけ入力欄を出す）
- `register` はテナント内での重複のみを弾く。プロビジョニングは新規テナントのため
  グローバルな重複チェックを廃止
- マイグレーション: `prisma/migrations/20260828010000_tenant_scoped_user_email/`

## D-03 ホスティング: GCP（Cloud Run + Cloud SQL）

**決定**: GCP の Cloud Run（コンテナ）＋ Cloud SQL for PostgreSQL、東京リージョン。

**理由**: エンタープライズ案件が確定しているためPaaSは審査面で不適。
AWS/GCP の二択では、コンテナをそのまま載せられ運用工数が小さいGCPを採る。

**実装上の含意（D-01 と強く相互作用する）**:
- **接続数の上限に注意**。Cloud Run は負荷でインスタンスが増え、
  各インスタンスが Prisma のプールを持つため Cloud SQL の接続上限を超えやすい。
  Cloud Run の最大インスタンス数を制限し、Prisma の `connection_limit` を明示する
- PgBouncer（transaction モード）を挟む場合、`SET LOCAL` はトランザクション内なので安全だが、
  Prisma のプリペアドステートメントと衝突するため接続文字列に `pgbouncer=true` が必要
- Cloud Run から Cloud SQL へは Direct VPC egress もしくは Cloud SQL Auth Proxy を使い、
  **パブリックIPを開けない**（エンタープライズのセキュリティ審査対策）
- `config.ts` と標準 `postgresql://` 接続に閉じる方針は維持する（クラウド固有SDKを足さない）

## D-04 認証: 招待メール方式

**決定**: 初期パスワードの手渡しをやめ、招待メール（トークン付きURL）で本人にパスワードを
設定させる。同じ基盤でパスワードリセットも提供する。

**実装上の含意**:
- メール配信サービスの選定・契約とドメイン認証（SPF/DKIM/DMARC）が必要
- 招待トークン・リセットトークンは**リフレッシュトークンと同様にハッシュのみDB保存**し、
  有効期限（招待7日 / リセット1時間程度）と使い捨て（使用後失効）を必ず実装する
- 通知機能（設定画面に項目だけ存在）でも同じメール基盤を使うため二重投資にならない

## D-05 課金: 実装しない（請求書払い・PLGではない）

**決定**: 課金体系は「施設数 × 客室数レンジ」を営業上の建て付けとするが、
**システムへの課金実装（Stripe等）・プラン上限のenforceは行わない**。

**理由**: 請求書払いかつPLGではないため、システムで決済・上限管理をする必要がない。

**含意**: `Tenant` に plan / 上限フィールドも当面追加しない。
契約情報は契約書と営業側の管理で足りる（nullable列の後付けは容易なため、必要になってからでよい）。

## D-06 データ保持期間: テナントごとに設定可能にする

**決定**: 監査ログ・実績データの保持期間を**テナント単位の設定項目**とする（当初案の
システム固定から変更）。

**理由**: 顧客はそれぞれ別会社であり、内部統制で要求される保持期間が異なるため。

**実装上の含意**:
- `Tenant` に保持期間フィールドを追加し、既定値をシステム側で定義する
- 掃除ジョブはテナントごとの設定を参照して削除する
- 設定画面の「データ保持期間」は現在ダミー（保存するだけで何もしない）なので、
  この実装と同時に**実際に効く**ようにする

## D-07 過去実績データ: 当面3年、ただし増える前提で設計する

**決定**: 受け入れ上限は当面3年（約1100行）のままとするが、
**将来的に行数が増える前提で、非同期ジョブ化に移行しやすい構造にしておく**。

**実装上の含意**:
- 上限をコード内の定数ではなく設定値として外に出す
- 同期処理のままでも、取り込み処理を「検証」と「書き込み」に分離しておき、
  ジョブ化する際に書き込み部分だけをワーカーへ移せるようにする
- Phase 4 のバッチ基盤と同時に非同期化する

## D-08 テナント識別: サブドメイン方式

**決定**: `<tenant>.example.com` のサブドメインでテナントを識別する。

**理由**: D-02（テナント単位の一意）でログイン時にテナント特定が必要になるが、
サブドメインで解決すればログイン画面にテナントコード欄を足さずに済む。
顧客ごとのブランド感も出る。

**実装状況**: 実装済み（PR #39）。`APP_BASE_DOMAIN` を設定すると有効になる（未設定の
ローカル開発では従来どおり組織コードで指定）。

- `src/lib/tenantResolver.ts`: Host からテナントコードを取り出す純粋関数（テスト13件）。
  多段サブドメイン・予約サブドメイン（www/api/app/admin 等）・`...example.com.evil.jp`
  のような詐称ドメインを弾く
- CORS はベースドメイン配下の1段サブドメイン（https のみ）を動的に許可
- 存在しない／他社のサブドメインでのログインは、テナントの存在を推測させないため
  認証失敗と同じメッセージを返す
- Cloud Run 等のプロキシ配下で Host とクライアントIPを正しく扱うため
  本番では `trust proxy` を有効にした（レート制限が全リクエストを同一IP扱いする問題も解消）

## D-09 団体客レベニュー影響ルール: プリセット方式

**決定**: `GroupBooking.revenueImpactRule` をプリセット選択式にする（自由記述はしない）。
ただし**プリセットの内容は今後変わる前提**とする。

**実装上の含意**:
- プリセットのキーはコード側（L1 システム固定）で定義し、
  DBには `{ presetKey, params, note }` の形で保存する
- プリセット追加時にマイグレーション不要な構造にしておく
- 集計・比較ができることが価値なので、`note` は補足に留め、判定ロジックに使わない

## D-10 OTAチャネル・レビューソース: マスタテーブル化する

**決定**: 現在コード上の暗黙の語彙（楽天トラベル・じゃらん・一休 等）を
テナントごとのマスタテーブルへ移行する。

**理由**: 会社ごとに取引チャネルが異なるSaaS展開のため。

**実装上の含意**:
- 優先度は高くない（D-01〜D-04 の後）。既定値をシステム側で用意し、
  **テナント作成時に自動投入して初期設定の手間を増やさない**こと
- 既存の文字列データからの移行スクリプトが必要
