# 開発への参加

このリポジトリで作業する前に、次を読んでください。

1. **`AGENTS.md`** — 必須ルール（認証・テナント分離・入力検証・監査ログ・アーキテクチャの境界）とコミット規約。例外はありません
2. **`README.md`** — セットアップ、コマンド、API 一覧
3. **`docs/改善計画.md`** — 未解消の所見と対応中のタスク（実装状況を書く前に必ず確認）
4. **`要件定義書.md`** §6 — 画面ごとの実装状況

## 作業の流れ

1. Issue を確認し、作業するものを決める（新しい所見は Issue にしてから）
2. ブランチを切る。コミットは修正単位で分け、件名は Conventional Commits ＋ Issue 番号・タスク ID（例 `fix(backend): … (#78)`）
3. スキーマを変えたら `prisma migrate dev` でマイグレーションを作ってコミットする（`db:push` は使わない。DB を初期化する操作は禁止）
4. コミット前に検証を通す（AGENTS.md の「コミット前に必ず全部通すこと」。Claude Code では `/check`）
5. プルリクエストはテンプレートに沿って書く。CI（型チェック・lint・テスト・マイグレーションのドリフト検出・E2E・Docker）がすべて通ってからレビューを依頼する

## 困ったとき

- セキュリティの問題は `SECURITY.md` の手順で非公開に報告してください
- 本番の運用は `docs/運用手順書.md`
