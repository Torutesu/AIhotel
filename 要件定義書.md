要件定義書（Requirements Definition Document）
AIレベニュー管理システム v1.0
作成日：2026/7/4　対象読者：開発チーム / クライアント（藤田観光・蘭賀様）
1. プロジェクト概要
項目内容プロジェクト名AI Revenue Management System目的ホテルの収益（レベニュー）を、AIによる需要予測・価格最適化・実績分析で最大化する管理システムを提供する初期クライアント藤田観光提供形態Webアプリケーション（SaaS想定、マルチテナント対応）フロントエンド稼働中（Vercel: hotel-price.vercel.app、Next.js）バックエンドこれから新規構築（本書のスコープ）
2. 背景・目的
現状、藤田観光を含む各ホテルの収益管理は担当者の経験と手動集計に依存している。本システムは以下を実現する。

日次・月次のKPI（ADR、稼働率、REV-Per等）を自動集計・可視化
需要予測とAIによる価格最適化で、稼働率とADRの最適バランスを提案
競合ホテルの価格動向を自動収集し比較分析
異常値・要注意事項をアラートとして自動検知
AIによる自然言語での状況説明（インサイト・サマリー）を自動生成

3. スコープ
3.1 対象範囲（In Scope）

ダッシュボード、ダイナミックプライシング、日別分析、各種分析、レポート、設定の6機能領域のバックエンドAPI
PMS／サイトコントローラー／OTA連携基盤（アダプター設計まで。個別PMS実装は連携先確定後）
需要予測・価格最適化AIエンジン
AIコメント生成（Claude API利用）
マルチテナント対応の認証・データ分離

3.2 対象外（Out of Scope、現時点）

サイトコントローラー・PMSへの自動反映（価格の書き戻し）※将来検討
決済・課金システム
モバイルネイティブアプリ
多言語対応（日本語のみ）

4. 想定ユーザー・ロール
ロール権限想定利用者admin全機能＋テナント設定システム提供側（蘭賀・田野）manager分析閲覧＋設定変更（価格戦略・重み付け等）ホテル支配人・レベニューマネージャーoperator閲覧＋イベント情報登録現場フロント担当
5. 機能要件
5.1 ダッシュボード
ID要件課題管理表対応NoF-DASH-01月別KPI進捗状況を表示（室料売上・販売室数・ADR・稼働率・REV-Per・宿泊人数・DOR・客単価）10, 11, 17F-DASH-02予算比・前年比を「本日まで」「累計進捗」「年度累計」の3軸で表示17F-DASH-03稼働・ADR月間推移グラフ（実績→予測の連続表示、曲線）7, 15F-DASH-04月初比較・日付比較（任意日付選択可）8, 16、藤田観光F5F-DASH-05アラート表示（赤＝即修正、黄＝経過観察）＋該当画面へのリンク遷移9, 14, 28F-DASH-06AIまとめ（自然言語サマリー）18、藤田観光F1-3
5.2 ダイナミックプライシング
ID要件課題管理表対応NoF-DP-01日別価格カレンダー（現在価格・推奨ADR・料金ランク表示、月選択）24, 25, 31, 32, 33F-DP-02価格戦略の重み付け設定（稼働率⇄ADR、競合追従度）21, 30、藤田観光F11-12F-DP-03AI予測値へのリセット機能藤田観光F12F-DP-04月間着地シミュレーション（ADR＋REV-Per併記）29, 30F-DP-05需要予測レベル表示（5段階）藤田観光F15F-DP-06競合平均価格の詳細分析（グラフ）17（旧）, 22F-DP-07イベント情報・外部要因の表示＋オペレーター登録26, 27
5.3 日別分析
ID要件課題管理表対応NoF-DAILY-01ブッキングカーブ表示（X軸：宿泊日までの日数、右肩上がり）36, 37F-DAILY-02週末定義＝金・土（チェックイン日基準）でのインサイト生成38F-DAILY-03競合ホテルとの価格比較（利用人数別：1名／2名／3名以上、切替可）40, 41
5.4 各種分析
ID要件課題管理表対応NoF-ANA-01利用人数別パフォーマンス（構成比・客単価。稼働率は表示しない）47, 48F-ANA-02競合分析（"平均"表現は使わず、ホテル別の販売価格として表示）51F-ANA-03年間推移（クォーター単位ではなく月単位）46, 49F-ANA-04口コミ評価点の収集・反映（Google, TripAdvisor等）45, 46F-ANA-05外部要因分析タブ（マーケット動向）藤田観光F1
5.5 レポート
ID要件F-REP-01月次レポート自動生成（売上サマリー）F-REP-02レポートのPDF/Excel出力
5.6 設定
ID要件課題管理表対応NoF-SET-01ホテル単位の初期設定（週末定義、部屋タイプ等）12F-SET-02料金ランクマスタ管理（最大40段階）16（藤田観光）F-SET-03競合ホテル登録（5社程度、OTA別URL）68, 74F-SET-04予算データ登録・更新-F-SET-05団体客レベニュー影響ルールの設定53（最新）
6. 非機能要件
カテゴリ要件性能ダッシュボード表示は2秒以内（キャッシュ利用）可用性99.5%以上（月間ダウンタイム約3.6時間以内）セキュリティTLS1.3必須、保存データ暗号化、テナント間データ分離必須拡張性ホテル数・テナント数の増加に対しスケールアウト可能な構成監査性全ての設定変更・重み付け変更を操作ログとして記録可搬性AWS/GCPどちらでも構築可能な設計（特定ベンダーロックインを避ける）
7. 制約事項・前提条件

PMS・サイトコントローラーの具体的な製品名とAPI仕様は未確定（クライアント確認待ち）。確定次第、個別アダプターを実装。
OTAスクレイピング対象は楽天トラベル・じゃらんnet・一休.com・Expedia・Agodaの5社（URLはクライアントより別途提供予定）。
過去実績データ（最低2年分）の提供形式は未確定。
団体客のレベニュー影響ルールはホテルごとに異なる可能性があり、協議中（課題No.53）。

8. マイルストーン（概算）
フェーズ内容期間目安Phase 1MVP（ダッシュボード＋KPI基盤）3-4週間Phase 2ダイナミックプライシング2-3週間Phase 3日別分析・各種分析・競合データ2-3週間Phase 4外部連携（PMS/サイトコントローラー/OTA）・需要予測高度化クライアントからの仕様提供に依存Phase 5マルチテナントSaaS化2-3週間
※Claude Code等のAI支援開発を前提とした期間。Phase 4はクライアント側の情報提供速度がボトルネックになる。

詳細仕様書（内部設計書）
※本章は実装（backend/src, backend/prisma/schema.prisma）に同期済み（2026/7/5更新）。実装が正、本書はそれに追従する。
1. システム構成
```
Frontend (Vercel/Next.js 15)
↓ REST API（JWT Bearer、/api/v1/ プレフィックス）
Backend: Express + TypeScript + Prisma ORM
↓ コンテナ実行（クラウド非依存: AWS ECS Fargate または GCP Cloud Run のどちらでも可）
PostgreSQL 16（マネージド: AWS RDS または GCP Cloud SQL。DATABASE_URL環境変数のみで接続先を切替、特定クラウドSDKに非依存）
↓（Phase 4で導入予定）
バッチジョブランナー（Node系ジョブランナー、またはAWS EventBridge Scheduler / GCP Cloud Scheduler等のマネージドスケジューラを想定。Redis/Celeryは現時点未導入）
↓（Phase 4で導入予定）
Claude API（AIコメント生成）／ 自前MLモデル（需要予測・価格最適化）　※現状は ai_comments / ai_price_recommendations ともにシードデータ
```
CI: GitHub Actions（.github/workflows/ci.yml）。全PR・main／production-readinessへのpushで type-check・backendテスト（Vitest）・frontend/backendビルドを必須ゲートとして実行。

2. データベース仕様（サマリー）
実装: backend/prisma/schema.prisma（Prisma ORM、PostgreSQL 16）。Tenantを最上位としたマルチテナント構成で、AuditLog以外の全業務モデルがtenantId（AuditLog/Userはシステム管理ユーザーのためnullable）を保持し、クエリは必ずtenantIdで絞り込む。モデル数24（テーブル名はPrisma既定によりモデル名と一致、例: "Tenant"）。
主要モデル一覧：
テーブル役割Tenantマルチテナント最上位（tenants: 全ドメインモデルの起点）Userユーザー（role: ADMIN/MANAGER/OPERATOR。ADMINはtenantId nullでテナント横断可）RefreshTokenリフレッシュトークン（生トークンではなくSHA-256ハッシュを保存し、ローテーション運用）Hotelホテルマスタ（totalRooms・weekendDays＝週末定義（デフォルト金・土）等）RoomType部屋タイプマスタ（capacity・count等）PriceRank料金ランクマスタ（最大40段階=F-SET-02、price1P/price2P/price3Pの1〜3名価格）PricingStrategyConfig価格戦略の重み付け設定（weightOccupancy/weightAdr/weightCompetitor=F-DP-02）DailyData日別実績（occupancy・adr・revPar・totalRevenue・soldRooms・guests等、KPIの元データ）DailyRoomData部屋タイプ別の日別実績（priceRank・price1P〜3P・soldRooms・revenue）MonthlyBudget月次予算・前年実績（F-SET-04/F-DASH-02）BookingCurveDataブッキングカーブ用予約積上データ（stayDate×daysBefore=F-DAILY-01）OtaChannelDataOTAチャネル別実績・キャンペーンフラグAiPriceRecommendationAI推奨価格・需要レベル（demandLevel=5段階=F-DP-05。現状シードデータ、Phase 4でMLモデルに接続）MonthlyLandingSimulation月間着地シミュレーション（F-DP-04）Alertアラート（severity: RED/YELLOW、linkTab=遷移先タブ=F-DASH-05）AiCommentAIコメント・インサイト（section単位。現状シードデータ、Phase 4でClaude API接続=F-DASH-06）KpiSnapshot月初等のKPIスナップショット（月初比較・日付比較=F-DASH-04）Campaignキャンペーン参画データ（channel・source等）Competitor競合ホテルマスタ（otaUrls=OTA別URL=F-SET-03）CompetitorPriceData競合価格データ（1〜3名価格。dataSource=scraping/manual、現状manual/seedのみ）ReviewScore口コミ評価点（source: google/tripadvisor等。現状シードデータ、収集バッチはPhase 4=F-ANA-04）GroupBooking団体客予約・レベニュー影響ルール（revenueImpactRuleはホテルごとに異なるためJsonで柔軟に保持=F-SET-05）Event外部要因・イベント（オペレーター登録=F-DP-07）AuditLog操作ログ（全設定変更・重み付け変更等を記録。§6監査性要件に対応）
主なenum: UserRole(ADMIN/MANAGER/OPERATOR)、DemandLevel(A/B/C/D/E＝需要予測5段階)、AlertSeverity(RED/YELLOW)、AlertStatus(OPEN/ACKNOWLEDGED/RESOLVED)。
マイグレーションは backend/prisma/migrations 配下で管理し、開発は `prisma migrate dev`、本番は `prisma migrate deploy` を使用。シードは backend/prisma/seed.ts（冪等、再実行可）。

3. API仕様（実装済みエンドポイント一覧）
実装: backend/src/routes/*.ts。全エンドポイントは `/api/v1/` プレフィックスを持ち、`/auth/login` と `/auth/refresh` を除き認証必須（JWT Bearer）。hotelIdを伴うエンドポイントは requireHotelAccess によりテナント・ホテル単位のアクセス制御を追加適用する（C-2/C-3）。エンドポイント数は認証系6・ホテル系5・ダッシュボード系4・プライシング系4・日別分析系2・各種分析系3・設定系4の計28。
領域Method/Path概要認証POST /api/v1/auth/loginログイン（JWTアクセストークン・リフレッシュトークン発行、専用レート制限あり）認証POST /api/v1/auth/refreshリフレッシュトークンによるアクセストークン再発行（ローテーション）認証POST /api/v1/auth/registerユーザー登録（ADMIN専用）認証POST /api/v1/auth/logoutログアウト（対象リフレッシュトークンを無効化）認証POST /api/v1/auth/logout-all全セッションログアウト認証GET /api/v1/auth/meログイン中ユーザー情報取得ホテルGET /api/v1/hotelsホテル一覧（ADMINは全件、それ以外は自テナントのみ）ホテルGET /api/v1/hotels/:idホテル詳細（自ホテルまたはADMINのみ）ホテルPOST /api/v1/hotelsホテル作成（ADMIN専用）ホテルPUT /api/v1/hotels/:idホテル更新（ADMIN専用）ホテルDELETE /api/v1/hotels/:idホテル削除（ADMIN専用）ダッシュボードGET /api/v1/dashboard/kpi月別KPI取得ダッシュボードGET /api/v1/dashboard/kpi/comparison月初/日付比較ダッシュボードGET /api/v1/dashboard/alertsアラート一覧ダッシュボードGET /api/v1/dashboard/ai-summaryAIまとめ取得（現状シードデータ）プライシングGET /api/v1/pricing/calendar日別価格カレンダープライシングGET /api/v1/pricing/strategy価格戦略の重み付け取得プライシングPUT /api/v1/pricing/strategy重み付け更新（MANAGER以上）プライシングGET /api/v1/pricing/simulation月間着地シミュレーション取得日別分析GET /api/v1/daily/booking-curveブッキングカーブ取得日別分析GET /api/v1/daily/competitor-prices競合価格取得（1〜3名価格）各種分析GET /api/v1/analysis/monthly年間の月次推移取得各種分析GET /api/v1/analysis/competitor競合分析取得各種分析GET /api/v1/analysis/reviews口コミ評価点取得（現状シードデータ）設定GET /api/v1/settings/price-ranks料金ランク一覧取得設定POST /api/v1/settings/price-ranks料金ランク作成（MANAGER以上）設定PUT /api/v1/settings/price-ranks/:id料金ランク更新（MANAGER以上）設定DELETE /api/v1/settings/price-ranks/:id料金ランク削除（MANAGER以上）
このほか認証不要のヘルスチェック（/api/v1配下ではない）として GET /health、GET /api/health がある。
※フルAPI仕様（リクエスト/レスポンススキーマ含む）は開発着手時にOpenAPI(Swagger)定義として別途生成し、dev側と同期する。

4. バッチ処理仕様（Phase 4で導入予定）
現時点でバッチ実行基盤（Redis/Celery等のジョブキュー）は未導入。DBスキーマ（AiPriceRecommendation, CompetitorPriceData, ReviewScore, AiComment等）とAPIの器は用意済みで、Phase 4でPMS/OTA連携先が確定次第、以下のジョブを設計・実装する。実行基盤はバックエンドと同じくクラウド非依存とし、Node系ジョブランナー、またはAWS EventBridge Scheduler + ECS Task／GCP Cloud Scheduler + Cloud Run Jobs等のマネージドスケジューラの利用を想定する。
時刻ジョブ説明01:00PMSデータ取込前日実績取込→DailyData/DailyRoomDataへ反映02:00OTAスクレイピング競合価格取得→CompetitorPriceDataへ反映03:00ブッキングカーブ更新予約積上データ更新→BookingCurveDataへ反映04:00需要予測実行向こう6ヶ月分再計算→AiPriceRecommendationへ反映05:00価格最適化実行推奨価格・ランク更新→AiPriceRecommendationへ反映06:00AIコメント生成Claude API呼び出し、各セクションのコメント更新→AiCommentへ反映06:30アラート生成閾値チェック→Alertへ反映

5. エラーハンドリング方針
実装: backend/src/middlewares/errorHandler.ts, backend/src/utils/response.ts。

全APIは統一レスポンス形式で返却する。成功時は {success: true, data, message?, meta?}、失敗時は {success: false, error: string, errors?: [{field, message}]}（errorsはZodバリデーションエラー等、フィールド単位のエラーがある場合のみ付与）。旧仕様で想定していた {error_code, message, detail} 形式は不採用とし、上記に統一済み。
Prismaの既知エラー（一意制約違反=409、レコード未検出=404、外部キー制約違反=400等）はerrorHandlerでHTTPステータス・日本語メッセージに変換する。
外部連携（PMS/OTA、Phase 4）失敗時はリトライ3回→Slack通知→手動介入フラグを設計方針として維持する（実装はPhase 4）。
AI推論失敗時は前回値をフォールバック表示し、UIに「予測更新失敗」バッジを表示する方針を維持する（AiPriceRecommendation/AiCommentの生成自体はPhase 4でClaude API/MLモデルに接続するまではシードデータを表示）。