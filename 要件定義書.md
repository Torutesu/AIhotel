# 要件定義書
## AIレベニュー管理システム（AI Revenue Management System）

**作成日：2026/7/5　バージョン：2.0（実装反映版）**
**対象読者：開発チーム（Claude Code含む） / クライアント（藤田観光・蘭賀様）**

> 本書はv1.0（構想段階）から、実際にバックエンド構築が進行し技術スタックが確定した
> 状態を反映した改訂版です。Claude Codeがこのリポジトリで作業する際は、まず本書と
> `AGENTS.md` / `CLAUDE.md` を読んでから着手してください。
>
> **本書は雛形（living document）である。** クラウド事業者・BaaS・テーブル条件など
> 「8.1 未確定事項と吸収方針」に挙げた項目は確定次第、本書と実装を更新する。

---

## 目次

1. プロジェクト概要
2. 現在の実装ステータス
3. 背景・課題（課題管理表より）
4. スコープ
5. 想定ユーザー・ロール
6. 機能要件
7. 非機能要件
8. 技術スタック（確定）
9. データベース設計
10. API仕様
11. Claude Codeでの開発の進め方
12. 今後のロードマップ
13. 課題トレーサビリティ
14. 用語集

---

## 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| プロジェクト名 | AI Revenue Management System |
| 目的 | ホテルの収益（レベニュー）を、AIによる需要予測・価格最適化・実績分析で最大化する管理システムを提供する |
| 初期クライアント | 藤田観光 |
| 提供形態 | Webアプリケーション（マルチテナントSaaS） |
| フロントエンド | Next.js（Vercelでホスティング中： hotel-price.vercel.app） |
| バックエンド | Express + TypeScript + Prisma（構築中、Critical/Warning指摘は修正済み） |

## 2. 現在の実装ステータス

前回までの総点検・修正作業により、以下が完了している。Claude Codeは**この状態を前提**に作業を進めること（ゼロから設計し直す必要はない）。

### 完了済み
- Prismaスキーマ：Tenantを最上位としたマルチテナント構成、24モデル実装済み
- 認証・認可：JWT + リフレッシュトークン（SHA-256ハッシュ保存）、全データルートに認証＋`requireHotelAccess`（テナント分離）を適用済み
- ロール：ADMIN / MANAGER / OPERATORの3種に統一済み
- APIエンドポイント：28本実装済み（ダッシュボードKPI、価格カレンダー、重み付け設定、着地シミュレーション、ブッキングカーブ、競合比較、月次推移、料金ランクCRUD等）
- 入力値検証：全ルートにzod適用済み
- 監査ログ：設定変更系（MANAGER以上）で記録
- フロントエンド：認証プロバイダー・ログイン画面を追加し、ダッシュボード／プライシング／日別分析タブを実APIに接続済み（モックへのフォールバックなし）
- CI/CD：GitHub Actionsで型チェック→テスト→両ビルドをPR時に必須化
- Docker：開発用（`docker-compose.dev.yml`）と本番用（`backend.Dockerfile` / `frontend.Dockerfile`）を分離
- テスト：バックエンドユニットテスト22件（認証ライブラリ・バリデータ）
- エージェント最適化：`AGENTS.md`（本体）＋`CLAUDE.md`（薄いラッパー）、スラッシュコマンド3種（`/check` `/db-migrate` `/pre-pr`）、危険操作ブロックフック、`MEMORY.md`索引＋メモリファイル群

### 未実施・要対応
- **実DB上でのE2E動作確認が未実施**（Docker起動→マイグレーション→seed→ログイン〜画面表示の一連が未検証）
- `production-readiness`ブランチのmainへの未マージ（PR未作成）
- 本番用シークレット（`JWT_SECRET` 32文字以上、`DATABASE_URL`）未設定
- PMS／サイトコントローラー連携（クライアントからの仕様提供待ち）
- OTAスクレイピング（対象5社のURL提供待ち）
- 需要予測・価格最適化AIモデルの実装（現状はダミー値／ルールベース想定）
- AIコメント生成（Claude API連携）
- バッチ処理基盤（スケジューラ）

## 3. 背景・課題（課題管理表より）

本プロジェクトの要件は、藤田観光との打ち合わせで作成された課題管理表
（`課題管理表_スケジュール20251231.xlsx`、`ホテルプライシング.pdf`、
`hotel_revenue_2025-11-25最新.pdf`）に基づく。主な論点は以下の4領域に整理される。

| カテゴリ | 主な論点 | ステータス（UI側） |
|---------|---------|-------------------|
| ダッシュボード | KPI進捗の月別表示、アラート、AIまとめ、月初/日付比較 | CLOSE多数（UI実装済み） |
| ダイナミックプライシング | 重み付け設定、料金ランク、着地シミュレーション、イベント情報 | 一部ONGOING/PENDING |
| 日別分析 | ブッキングカーブ、週末定義（金土）、競合価格比較 | CLOSE多数 |
| 各種分析 | 利用人数別パフォーマンス、競合分析、口コミ評価、年間推移 | 一部PENDING |
| 設計・連携 | PMS/サイトコントローラー/OTA連携、セキュリティ、インフラ | OPEN（クライアント確認待ち） |
| 事業計画 | 契約形態、料金プラン、開発費用概算 | ビジネス側協議中 |

課題管理表の個別No.と実装の対応関係は「13. 課題トレーサビリティ」を参照。

## 4. スコープ

### 4.1 対象範囲（In Scope）
- ダッシュボード、ダイナミックプライシング、日別分析、各種分析、レポート、設定の6機能領域のバックエンドAPI（実装済み・継続拡張）
- PMS／サイトコントローラー／OTA連携（アダプター設計＋連携先確定後の実装）
- 需要予測・価格最適化エンジン
- AIコメント生成（Claude API）
- マルチテナント対応の認証・データ分離（実装済み）

### 4.2 対象外（Out of Scope、現時点）
- サイトコントローラー・PMSへの自動反映（価格の書き戻し）
- 決済・課金システム
- モバイルネイティブアプリ
- 多言語対応（日本語のみ）

## 5. 想定ユーザー・ロール

| ロール | 権限 | 想定利用者 |
|--------|------|-----------|
| ADMIN | 全機能＋テナント設定＋ユーザー登録 | システム提供側（蘭賀・田野） |
| MANAGER | 分析閲覧＋設定変更（価格戦略・重み付け等）＋監査ログ対象操作 | ホテル支配人・レベニューマネージャー |
| OPERATOR | 閲覧＋イベント情報登録 | 現場フロント担当 |

## 6. 機能要件

### 6.1 ダッシュボード
| ID | 要件 | 課題No | 実装状況 |
|----|------|--------|---------|
| F-DASH-01 | 月別KPI進捗状況（室料売上・販売室数・ADR・稼働率・REV-Per・宿泊人数・DOR・客単価） | 10, 11, 17 | 実装済み |
| F-DASH-02 | 予算比・前年比を「本日まで」「累計進捗」「年度累計」の3軸で表示 | 17 | 実装済み |
| F-DASH-03 | 稼働・ADR月間推移グラフ（実績→予測の連続表示） | 7, 15 | 実装済み |
| F-DASH-04 | 月初比較・日付比較（任意日付選択） | 8, 16 | 実装済み |
| F-DASH-05 | アラート表示（赤/黄）＋該当画面へのリンク遷移 | 9, 14, 28 | 実装済み（DB上） |
| F-DASH-06 | AIまとめ（自然言語サマリー） | 18 | 表示API/UI実装済み（生成はseedデータ、Claude API連携はPhase 4） |

### 6.2 ダイナミックプライシング
| ID | 要件 | 課題No | 実装状況 |
|----|------|--------|---------|
| F-DP-01 | 日別価格カレンダー（現在価格・推奨ADR・料金ランク、月選択） | 24, 25, 31-33 | 実装済み |
| F-DP-02 | 価格戦略の重み付け設定（稼働率⇄ADR、競合追従度、合計100%） | 21, 30 | 実装済み |
| F-DP-03 | AI予測値へのリセット機能 | - | 未確認（要テスト） |
| F-DP-04 | 月間着地シミュレーション（ADR＋REV-Per併記） | 29, 30 | 実装済み |
| F-DP-05 | 需要予測レベル表示（5段階） | - | ルールベースのダミー値、AIモデル未実装 |
| F-DP-06 | 競合平均価格の詳細分析 | 22 | 実装済み（"平均"表現は使用しない仕様） |
| F-DP-07 | イベント情報・外部要因の表示＋オペレーター登録 | 26, 27 | 実装済み（API＋プライシングタブUI） |

### 6.3 日別分析
| ID | 要件 | 課題No | 実装状況 |
|----|------|--------|---------|
| F-DAILY-01 | ブッキングカーブ表示（宿泊日までの日数、右肩上がり） | 36, 37 | 実装済み |
| F-DAILY-02 | 週末定義＝金・土でのインサイト生成 | 38 | 実装済み |
| F-DAILY-03 | 競合価格比較（利用人数別：1名/2名/3名以上） | 40, 41 | 実装済み |

### 6.4 各種分析
| ID | 要件 | 課題No | 実装状況 |
|----|------|--------|---------|
| F-ANA-01 | 利用人数別パフォーマンス（構成比・客単価。稼働率は非表示） | 47, 48 | 実装済み |
| F-ANA-02 | 競合分析（"平均"表現を使わない） | 51 | 実装済み |
| F-ANA-03 | 年間推移（月単位） | 46, 49 | 実装済み |
| F-ANA-04 | 口コミ評価点の収集・反映 | 45, 46 | 未着手 |

### 6.5 レポート・設定
| ID | 要件 | 実装状況 |
|----|------|---------|
| F-REP-01 | 月次レポート自動生成 | 未着手 |
| F-REP-02 | レポートのPDF/Excel出力 | 未着手 |
| F-SET-01 | ホテル単位の初期設定（週末定義等） | 実装済み |
| F-SET-02 | 料金ランクマスタ管理（40段階に統一） | 実装済み |
| F-SET-03 | 競合ホテル登録（5社、OTA別URL） | スキーマ対応済み、URL提供待ち |
| F-SET-04 | 予算データ登録・更新 | 実装済み |
| F-SET-05 | 団体客レベニュー影響ルール | 未着手（課題No.53、協議中） |

## 7. 非機能要件

| カテゴリ | 要件 | 実装状況 |
|---------|------|---------|
| 性能 | ダッシュボード表示2秒以内 | 未検証（キャッシュ層未導入） |
| 可用性 | 99.5%以上 | インフラ未確定 |
| セキュリティ | TLS1.3、保存データ暗号化、テナント分離必須 | テナント分離は実装済み、TLS/暗号化はインフラ確定後 |
| 拡張性 | ホテル数・テナント数増加に対応 | Tenantモデルで対応済み |
| 監査性 | 設定変更を操作ログとして記録 | 実装済み（監査ログ） |
| 可搬性 | AWS/GCPどちらでも構築可能 | 環境変数による抽象化を想定、クラウド未確定 |

## 8. 技術スタック（確定）

前回の総点検で「要件定義書はFastAPI、実装はExpress」という矛盾が発見されたため、
**実装済みのコストを踏まえてExpress + TypeScript + Prismaに正式決定した**。
今後この方針を覆す提案をする場合は、既存実装（認証基盤・28エンドポイント・
テストスイート）の書き直しコストを踏まえて判断すること。

| レイヤー | 技術 | 備考 |
|---------|------|------|
| フロントエンド | Next.js（App Router） | Vercelでホスティング |
| バックエンド | Express + TypeScript | 確定 |
| ORM | Prisma | 24モデル実装済み |
| DB | PostgreSQL | ローカル: Docker、本番: クラウド（RDS or Cloud SQL、未確定） |
| 認証 | JWT + リフレッシュトークン（SHA-256ハッシュ保存） | 実装済み |
| バリデーション | zod | 全ルート適用済み |
| テスト | vitest | ユニットテスト22件 |
| CI/CD | GitHub Actions | 型チェック→テスト→ビルドをPR必須化 |
| コンテナ | Docker（開発用/本番用Dockerfile分離） | 実装済み |
| クラウド | AWS または GCP（未確定） | 環境変数で抽象化する設計方針 |
| AIコメント生成 | Claude API（Anthropic） | 未実装 |
| 需要予測・価格最適化 | 未確定（scikit-learn / LightGBM等を想定） | 未実装、現状はダミー値 |

### データ保存方針
本番データはローカルディスクではなくクラウド上に保存する。DB接続文字列・
シークレット類は環境変数経由とし、AWS Secrets Manager / GCP Secret Manager
のどちらでも差し替え可能な設定読み込み層を用いる。レポート等のファイル生成物は
オブジェクトストレージ（S3 / GCS）に保存し、ローカルディスクへの永続化は
開発環境限定とする。

### 8.1 未確定事項と吸収方針

以下は現時点で未確定であり、確定を待たずに開発を進められるよう、
変更時の影響範囲をコード上の境界に閉じ込めている（ESLintで機械的に強制）。

| 未確定事項 | 選択肢 | 吸収する境界（変更箇所） |
|-----------|--------|------------------------|
| クラウド事業者 | AWS / GCP | `backend/src/lib/config.ts`（環境変数・シークレット読み込みの唯一の場所）と `docker/*.Dockerfile`。アプリコードにクラウドSDKを持ち込まない |
| DB/BaaS | マネージドPostgreSQL（RDS/Cloud SQL）/ Supabase（Postgres互換なのでDATABASE_URL差し替えのみ）/ Firebase（非Postgresのため要判断） | `backend/src/services/`（Prismaクライアントの import はこの層と lib/ のみに Lint で制限）。Firebase等の非SQL採用時も書き換えは services 層に限定される |
| テーブル条件・カラム定義 | クライアント協議中（団体客ルール、利用人数別実績の粒度等） | `backend/prisma/schema.prisma` ＋ マイグレーション。API入出力は zod スキーマ（`lib/validators.ts`）が契約なので、内部カラム変更はフロントに波及しない |
| 認証基盤 | 自前JWT（実装済み）/ 将来的にSupabase Auth等 | `backend/src/lib/auth.ts` と `middlewares/auth.ts`。ルート側は `authenticate`/`requireHotelAccess` の抽象に依存しており実装差し替え可能 |
| シークレット供給元 | .env / AWS Secrets Manager / GCP Secret Manager | `backend/src/lib/config.ts` のローダー部分のみ |

## 9. データベース設計

Prismaスキーマは24モデル、Tenantを最上位としたマルチテナント構成。
主要テーブル：

| テーブル | 役割 |
|---------|------|
| Tenant / Hotel / User | マルチテナント・ホテル・ユーザー管理（週末定義は Hotel.weekendDays） |
| RoomType / PriceRank | 部屋タイプ・料金ランクマスタ（40段階） |
| DailyData | 日別実績（KPIの元データ） |
| BookingCurveData | ブッキングカーブ用予約積上データ |
| OtaChannelData | OTAチャネル別実績・キャンペーンフラグ |
| MonthlyBudget | 月次予算・前年実績 |
| Competitor / CompetitorPriceData | 競合マスタ（OTA別URLは otaUrls）・競合価格 |
| Event | イベント・外部要因（オペレーター登録可 — F-DP-07） |
| PricingStrategyConfig | 価格戦略の重み付け設定 |
| AiPriceRecommendation | AI推奨価格・需要レベル |
| MonthlyLandingSimulation | 月間着地シミュレーション |
| Alert | アラート（赤/黄、リンク先） |
| AiComment | AIコメント・インサイト |
| KpiSnapshot | 月初等のKPIスナップショット |
| ReviewScore | 口コミ評価点 |
| GroupBooking | 団体客予約・レベニュー影響 |
| AuditLog | 監査ログ |

詳細なカラム定義はリポジトリ内 `prisma/schema.prisma` を正とする
（本書には転記しない。乖離を防ぐため、変更時は必ずスキーマファイルを更新すること）。

## 10. API仕様

主要28エンドポイントは実装済み。詳細はOpenAPI定義（未生成の場合はClaude Codeが
`prisma/schema.prisma`とルート実装から生成すること）を正とする。エラーレスポンス
形式は `{success, error, errors}` に統一済み。認証は全データルートで必須、
`requireHotelAccess`によるテナント分離を必ず適用する。

## 11. Claude Codeでの開発の進め方

### 11.1 参照すべきファイル（作業開始前に必ず読む）
1. `AGENTS.md` — プロジェクトの本体ルール（検証コマンドの順序、テナント分離の必須事項、確定済みドメイン値、コミット規約）
2. `CLAUDE.md` — `AGENTS.md`を取り込む薄いラッパー＋Claude Code固有の参照
3. `.claude/skills/hotel-revenue-rules/SKILL.md` — このプロジェクト固有の判断基準
4. `MEMORY.md` — 過去セッションで蓄積された学習・決定事項の索引
5. 本書（`要件定義書.md`）

### 11.2 開発フロー
1. 新機能・修正はブランチを切って作業する（`production-readiness`のような機能単位のブランチ名）
2. 新しいルートを追加する場合は必ず「認証＋テナント分離（`requireHotelAccess`）＋zod検証」の3点セットを適用する
3. 新しいPrismaモデルには必ず`tenantId`を持たせる
4. 破壊的マイグレーション（`migrate reset`、`--force-reset`等）は`.claude/hooks/guard-dangerous.sh`によりブロックされる。必要な場合は理由を明示してユーザーに確認を取る
5. コミットは指摘ID・課題No・機能IDのいずれかを含めて分割する（例：`C-2, C-3: 認証・テナント分離を全ルートに適用`）
6. 作業完了前に用意されているスラッシュコマンドを実行する：
   - `/check` — 型チェック→テスト→ビルドの検証パイプライン
   - `/db-migrate <name>` — スキーマ変更後のマイグレーション生成（tenantId必須チェック含む）
   - `/pre-pr` — main比較の差分をプロジェクトルールと照合し、マージ可否を報告
7. Phase 4（PMS/OTA/サイトコントローラー連携、AI予測モデル）に該当する機能は、
   クライアントからの仕様確定なしに「実装済み」として報告しない

### 11.3 検証の原則
- 実行して確認していない内容は「未検証」と明記する（推測での完了報告をしない）
- ローカルDB（Docker）が起動していない環境では、その旨を明記し、可能な範囲
  （型チェック・ユニットテスト・ビルド）で代替検証する
- 大きな実装作業は、設計・検証をClaude本体が担当し、独立した実装をサブエージェントに
  委譲してよい。委譲するかどうかは対象ファイル数・行数から判断し、小規模な変更
  （目安：10ファイル未満かつ200行未満）は直接実施したほうがトークン効率が良い

## 12. 今後のロードマップ

| フェーズ | 内容 | 状況 |
|---------|------|------|
| Phase 1 | MVP（認証・DB・ダッシュボードAPI） | **完了** |
| Phase 2 | ダイナミックプライシング | **完了**（イベント情報管理も実装済み） |
| Phase 3 | 日別分析・各種分析・競合データ | **概ね完了**（口コミ評価点のみ未実装） |
| Phase 3.5 | 実DB E2E検証・mainマージ・本番シークレット設定 | **次のアクション** |
| Phase 4 | PMS/サイトコントローラー/OTA連携、AI予測モデル、AIコメント生成 | クライアントからの仕様提供待ち |
| Phase 5 | マルチテナントSaaS化（プラン別制限、課金連携） | 未着手 |

## 13. 課題トレーサビリティ（抜粋）

| 課題No | 内容 | 対応テーブル/実装 | ステータス |
|--------|------|-------------------|-----------|
| 7 | 稼働・ADR月間推移 | DailyData | 実装済み |
| 9, 14, 28 | アラート（赤/黄、リンク遷移） | Alert | 実装済み |
| 12 | ホテル単位初期設定 | Hotel.weekendDays 等（設定タブ＋PUT /settings/hotel/:id） | 実装済み |
| 21, 30 | 重み付け設定 | PricingStrategyConfig | 実装済み |
| 26, 27 | イベント情報管理 | Event（/api/v1/events、オペレーター登録可） | 実装済み |
| 29 | 着地シミュレーション | MonthlyLandingSimulation | 実装済み |
| 36-38 | ブッキングカーブ、週末=金土 | BookingCurveData、Hotel.weekendDays | 実装済み |
| 40, 41 | 競合価格比較（利用人数別） | CompetitorPriceData（price1P/2P/3P） | 実装済み |
| 45, 46 | 口コミ評価点 | ReviewScore | スキーマ・APIのみ、収集未実装 |
| 47, 48 | 利用人数別パフォーマンス | DailyData.guests（人数区分別の粒度はテーブル条件確定待ち） | 部分実装 |
| 51 | 競合分析（"平均"表現不使用） | CompetitorPriceData | 実装済み |
| 53（最新） | 団体客レベニュー影響 | GroupBooking | スキーマのみ |
| 54 | OTAスクレイピング | Competitor.otaUrls | URL提供待ち |
| 56, 57 | サイトコントローラー/PMS連携 | アダプター未実装 | 仕様確認待ち |

## 14. 用語集

| 用語 | 説明 |
|------|------|
| ADR | Average Daily Rate（平均客室単価） |
| REV-Per | Revenue per Available Room（客室あたり収益、稼働率×ADR） |
| DOR | Days of Rest（宿泊人数/販売室数、1室あたり平均利用人数） |
| OTA | Online Travel Agent（楽天トラベル、じゃらん等） |
| PMS | Property Management System（ホテル運営管理システム） |
| 料金ランク | 40段階に統一された価格帯の区分 |
| テナント | このSaaSを利用する事業者単位（藤田観光等） |
