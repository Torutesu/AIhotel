# AIエージェント構成の設計方針（4E）

**ステータス: 方針承認済み。4E-1 / 4E-2 実装済み（2026/8/11）**
実装前レビュー必須（`docs/drive-gap-analysis.md` 決定#13）。
出典: 仕様書Ⅱ章「AIエージェント構成（技術要件）」「精度目標」「例外処理・フォールバック」、
機能一覧 F-005〜F-008、Ⅲ章6(1) レイテンシ要件。

---

## 0. この文書の目的

仕様書は4エージェント構成を「勾配ブースティング系または時系列Transformer系」と書いている。
そのまま受け取ると Python のMLスタックを別コンテナで立てる話になり、
「クラウド非依存・コンテナ1本」という現在の方針と衝突する。

実データを見て、**その前提が本当に必要かを先に検証した**。結論は §2。

---

## 1. まず事実確認：学習データは想像よりはるかに小さい

日次需要予測の学習サンプルは「1日 = 1行」である。手元の実データで数えると:

| 指標 | 実測値 |
|---|---|
| ReservationNight 明細行 | 47,263行 |
| **うち独立した宿泊日** | **119日** |
| データ期間 | 2025-01-01 〜 2026-08-10（実データは2025年1〜2月、他はseed） |
| 1施設・1年で得られる日次サンプル | **約365行** |
| 部屋タイプ別に分けた場合 | 365 × 16タイプ = 約5,800行/年 |

**これは機械学習の文脈では極端に小さい。**
勾配ブースティングの実力が出るのは数万〜数百万行の領域で、
数百行に対して適用すると、ハイパーパラメータ調整のコストに対して
正則化した線形モデルとの差がほとんど出ないか、むしろ過学習で悪化する。

さらに悪いことに、ホテル需要には**構造変化**がある（コロナ、インバウンド回復、
近隣の大型施設の開業）。3年前のデータを機械的に学習させると、
むしろ精度を落とす。実質的に使えるのは直近1〜2年 = **数百行**である。

### この事実から言えること

- 「毎日再学習」は正しい。ただし再学習の対象は**巨大モデルではなく小さなモデル**
- 精度を決めるのは**モデルの複雑さではなく特徴量の質**（曜日・特日・リードタイム・
  ブッキングカーブ形状・外部要因）。ここに投資すべき
- Python サイドカーを立てる技術的必然性が、**現時点では無い**

---

## 2. 提案：TypeScript内製 → 必要になったらPythonへ、の2段構え

### フェーズ1（今回実装する範囲）

すべて既存のExpressバックエンド内で完結させる。

| エージェント | 実装方針 |
|---|---|
| **需要予測** | 特徴量設計 ＋ **正則化線形回帰（Ridge）と勾配ブースティング木の内製実装**の2本立て。同じ特徴量で両方を学習し、検証誤差の小さい方を自動採用する |
| **価格決定** | ルール＋モデルのハイブリッド。予測稼働率・特日・外部要因・料金ランクマスタから推奨ランクを算出。**LLMは使わない**（仕様書明記） |
| **要因解説** | Claude API。`lib/` のアダプタ層に隔離し、controllers/services からは直接叩かない |
| **学習** | 承認/否認ログから特徴量重みを**定期バッチ更新**。都度の再学習はしない（F-007明記） |

**なぜ2本立てにするか**: どちらが勝つかはデータ次第で、事前には決められない。
両方を同じインターフェースで実装して**毎日の学習時に検証誤差で選ばせる**のが、
議論せずに答えを出す唯一の方法である。実装コストは木のほうが重いが、
どちらも数百行のデータなら学習は1秒未満で終わる。

### フェーズ2（フェーズ1で精度目標に届かなかった場合のみ）

Python の学習ワーカーを別コンテナで立て、ONNX で書き出したモデルを
Node 側が `onnxruntime-node` で読む。**バックエンド本体は TypeScript のまま**で、
AGENTS.md の「FastAPIへ移行しない」に抵触しない。

移行の判断基準を先に決めておく:
> 12ヶ月分の実データで交差検証し、**予測稼働率のMAEが±10%を超え続ける**なら
> フェーズ2へ。届いているならフェーズ1のまま運用する。

---

## 3. 4エージェントの責務と境界

```
                    ┌─────────────────┐
   PMS実績・オンハンド → │ ① 需要予測      │ → 日次の予測稼働率・確信度
   特日・外部要因      │   Forecaster    │
                    └─────────────────┘
                              ↓
                    ┌─────────────────┐
   料金ランクマスタ  → │ ② 価格決定      │ → 推奨ランクコード・推奨価格
   競合・在庫状況     │   PriceDecider  │   ＋確信度スコア
                    └─────────────────┘
                              ↓
                    ┌─────────────────┐
                    │  価格変更提案     │ ← 4Dの承認画面がここを読む
                    │  Proposal        │ → 承認/否認ログ
                    └─────────────────┘
                         ↓          ↓
              ┌──────────────┐  ┌─────────────────┐
              │ ③ 要因解説    │  │ ④ 学習          │
              │  Claude API  │  │  重みバッチ更新  │
              └──────────────┘  └─────────────────┘
                                        ↓
                                  ①②の特徴量重みへ反映
```

### 各エージェントが「触ってよいもの」

| | 入力 | 出力 | 触らないもの |
|---|---|---|---|
| ① 需要予測 | 実績・オンハンド・特日・外部要因 | 予測稼働率・確信度 | 価格。**価格を予測に混ぜない**（価格→需要→価格の循環を避ける） |
| ② 価格決定 | ①の出力・料金ランクマスタ・在庫 | 推奨ランク・確信度 | 実績の再集計（①の結果を信じる） |
| ③ 要因解説 | 予実・①②の出力 | 日本語コメント3種 | **数値の決定**。解説専用で、提案内容を変えない |
| ④ 学習 | 承認/否認ログ | 特徴量重みの更新 | モデル構造の変更（重みのみ） |

**③が数値に触らない**のは意図的な安全設計である。LLMの出力が価格に影響する経路を作らなければ、
LLMの誤りは「説明が変」で済み、「価格が壊れる」にはならない。

---

## 4. 確信度スコアの定義（仕様書は「付与する」としか書いていない）

仕様書は閾値を「稼働後キャリブレーション、当初は暫定値」としている。
**何を測った数字なのかは決めておかないと運用できない**ので、ここで定義する。

確信度 = 次の4つの積（各0〜1）:

| 因子 | 意味 | 低くなる条件 |
|---|---|---|
| `dataSufficiency` | 学習データの厚み | 同条件（曜日×季節×特日）の過去サンプルが少ない |
| `modelFit` | 直近の予測精度 | 直近30日の予測誤差が大きい |
| `stability` | 予約推移の安定性 | オンハンドが前年・直近トレンドから大きく外れている（**異常値検知**） |
| `horizonDecay` | 予測地平の減衰 | 遠い日付ほど下がる（180日先は当然低い） |

- 閾値の初期値: **0.70**。これ以上→自動書き込み継続、未満→**半自動モード**（オペレーター確認）
- 閾値はホテル単位で設定可能にする（稼働後のキャリブレーション用）
- 天変地異・大規模イベントのキャンセルは `stability` が落ちることで自動的に半自動へ倒れる

**確信度は「当たる確率」ではない**ことを画面にも明記する。仕様書Ⅲ章6(3)①の免責と整合させる。

---

## 5. 学習エージェントと 4D の依存関係（要判断）

学習エージェントの入力は**承認/否認ログ**で、これは 4D（承認・SC連携）の成果物である。
4E を 4D より先に実施する場合、ここだけ順序が逆になる。

**提案**: 価格変更提案とその判断ログの**データモデルだけを 4E に前倒しする**。

| 4E でやる | 4D でやる |
|---|---|
| `PriceChangeProposal`（提案・確信度・根拠） | 承認画面のUI |
| `ProposalDecision`（承認/否認・理由・実行者） | サイトコントローラーへの書き込み |
| 学習バッチが読む集計クエリ | 書き込みステータスの監視 |

こうすると 4E は自己完結し、4D は UI と SC 連携に集中できる。
モデルを二度設計せずに済む。

---

## 6. 権限（F-008）

「誰の承認・対話を学習の正とするか」は **ADMINのみ設定可能**。

- `User.isLearningSource`（Boolean）を追加し、ADMIN だけが変更できる
- 学習バッチは `isLearningSource = true` のユーザーの承認/否認のみを教師データにする
- 変更は `writeAuditLog()` の対象（誰が学習ソースを変えたかは監査対象）

理由: 新人オペレーターの誤操作が、そのままホテルの戦略として学習されるのを防ぐため。

---

## 7. Claude API の扱い（要因解説エージェント）

- **配置**: `backend/src/lib/claudeClient.ts`（アダプタ層）。
  services/controllers から直接 SDK を触らない（AGENTS.md のアーキテクチャ境界）
- **二次利用防止**: 仕様書Ⅲ章6(2)② の Opt-out を適用。
  Anthropic API はデフォルトで顧客データをモデル学習に使わないが、**設定として明示的に記録**し、
  クライアントに提示できる状態にする
- **送信データの最小化**: 個人情報は取込段階で排除済みだが、
  加えて**集計済みの数値と日付のみ**を送る。予約明細は送らない
- **APIキー未設定時**: コメント生成をスキップし、画面には「AIコメントは利用できません」と表示する。
  **モック文言へのサイレントフォールバックはしない**（AGENTS.md）
- **コスト**: 1ホテル1日あたり数十件のコメント生成。プロンプトキャッシュで固定部分を再利用する

---

## 8. 日次バッチ（Ⅲ章6(1)）

> PMS夜間バッチ検知後、深夜0時〜翌朝6時に日次の需要予測・価格ランク再計算を完了させる

今回作った取込基盤（`docs/pms-ingest-design.md` §E）と接続する:

```
PMS取込完了（IngestLog SUCCESS）
   → ① 需要予測の再学習・再予測（180日先まで）
   → ② 価格決定・提案生成
   → ③ 要因解説コメント生成
   → ④ 学習バッチ（週次。毎日は回さない）
```

- **取込完了をトリガーにする**（時刻固定ではなく）。夜間バッチの遅延に追随できる
- 全体の所要時間は数十秒〜数分の見込み（データが小さいため）。6時間の枠に対して余裕がある
- 起動方式は取込スケジューラと同じ仕組みを再利用する

---

## 9. 精度目標と、その測り方

仕様書の目標（フル稼働1年後測定）:

| 目標 | 測定方法 |
|---|---|
| 需要予測エラー率 ±10%以内 | 予測稼働率と実績稼働率のMAE。**予測時点別**に測る（7日前・30日前・90日前） |
| 売上 +15% | 導入前同月比。外部要因（インバウンド等）の影響を除けないため参考値 |
| 費用 50%削減 | オペレーターの作業時間。導入前ヒアリングでベースラインを取る必要あり |
| コメント承認率 90%以上 | 要因解説コメントの採用/修正/棄却をログに残して集計 |

**測定基盤を先に作る**。目標だけあって測れない状態を避けるため、
予測は必ず `modelVersion` と予測時点を添えて保存し、後から誤差を再計算できるようにする。

---

## 10. MLOpsモニタリング画面（仕様書 ④）

表示するもの:

- **承認率の推移**（週次）— AIの提案がどれだけ受け入れられているか
- **予測誤差の推移** — 予測時点別（7日前/30日前/90日前）のMAE
- **確信度の分布** — 半自動モードに落ちた割合
- **思考アジャスト進捗** — 学習バッチの実行履歴と、重みが更新された特徴量

「学習が進んでいる感」を出すための飾りは置かない。
**運用判断に使える数字だけ**を出す（閾値を上げ下げすべきか、半自動が多すぎないか）。

---

## 11. 実装順序

| ステップ | 内容 | 依存 |
|---|---|---|
| 4E-1 | 特徴量設計＋予測の評価基盤（`modelVersion`・予測時点・誤差集計） | **実装済み（2026/8/11）— §14** |
| 4E-2 | 需要予測エージェント（Ridge＋GBM内製、検証誤差で自動選択） | **実装済み（2026/8/11）— §15** |
| 4E-3 | 価格決定エージェント＋確信度スコア＋`PriceChangeProposal` モデル | 4E-2 |
| 4E-4 | 要因解説エージェント（Claude API アダプタ） | 4E-3 |
| 4E-5 | 学習エージェント（重みバッチ更新）＋`User.isLearningSource` | 4E-3 |
| 4E-6 | MLOpsモニタリング画面 | 4E-1〜5 |

---

## 12. レビューしてほしい点

1. **§2 の方針**（TypeScript内製で始め、精度が届かなければPythonへ）で進めてよいか。
   仕様書の「勾配ブースティング系または時系列Transformer系」から見ると軽く見えるが、
   データが数百行しかないという事実に基づく判断
2. **§5 の前倒し**（提案・判断ログのモデルだけ4Eに含める）でよいか
3. **§4 の確信度の定義**（4因子の積・初期閾値0.70）が運用感覚と合っているか
4. **§9 の測定**について、費用50%削減のベースライン（導入前の作業時間）を
   クライアントに確認する必要がある — ヒアリング項目に追加すべきか

---

## 13. クライアント確認事項（追加分）

1. 日次バッチの実行時間帯（PMS夜間バッチの完了時刻。仕様書も「初期設定時にヒアリング」としている）
2. 費用50%削減の測定ベースライン（導入前のレベニュー業務の作業時間）
3. 学習ソースとして正とするユーザー（誰の承認を学習するか）の初期設定
4. Claude API 利用について、ホテル側のセキュリティポリシー上の可否確認

---

## 14. 4E-1 の実装結果（2026/8/11）

### 作ったもの

| ファイル | 役割 |
|---|---|
| `services/forecast/features.ts` | 25特徴量の定義と組み立て（純関数）。時系列分割も含む |
| `services/forecast/featureContextService.ts` | 特徴量の材料をDBから一括取得。リーク防止の要 |
| `services/forecast/evaluation.ts` | 予測時点別の誤差集計（MAE / RMSE / 目標達成率 / バイアス） |
| `services/forecast/accuracyService.ts` | 予測の記録・実績突合・精度サマリ |
| `ForecastSnapshot` モデル | 「いつ時点で、どのモデルが、何を予測したか」の履歴 |
| `GET /api/v1/pricing/accuracy` | 精度の参照（MLOps画面・モデル選択の事後検証用） |

### 設計判断

- **欠損は0ではなく0.5で埋める**: 稼働率系の特徴量で欠損を0埋めすると、
  「まだデータが無い」と「本当に空っぽ」を区別できなくなる。
  欠損フラグを別特徴量にするのが定石だが、数百行のデータで特徴量数を倍にすると
  過学習が増えるため、分布の中心で埋める方を選んだ
- **季節性は円周に写す**: 月番号をそのまま入れると12月と1月が最も遠い値になる。
  年内進捗の sin/cos で年またぎの不連続を消した
- **分割は必ず時間順**: 需要予測でランダム分割すると未来で過去を予測する形になり、
  検証誤差が実力より良く出る
- **リーク防止**: `FeatureContext` は `predictedAt` を必須にし、
  オンハンド・残室は `capturedDate <= predictedAt`、実績は `stayDate < predictedAt` でのみ読む

### 実データで見つけた欠陥（修正済み）

過去日に対して予測を走らせたところ、59件すべてが「7日前まで」のバケットに入り、
MAE 29.7pt という数字が出た。原因は**リードタイムが負**（宿泊日より後に計算したもの）で、
最短バケットに落ちていたこと。これは予測ではなく後付けの計算なので、
混ぜると7日前予測の精度が歪む。

`horizonBucketOf` が負のリードタイムに `null` を返すようにし、
精度集計から除外して `excludedHindcasts` として件数だけ報告するようにした。
修正後は「測定対象0件・判定不能」と正しく出る（達成/未達を偽らない）。

### 疎通確認

- 未来142日分の予測を記録 → リードタイム1〜142日で各バケットに分散することを確認
- 過去59件は後付けとして記録されるが精度集計からは除外されることを確認
- 実績突合（`scoreForecastSnapshotsService`）が59件を実績と結びつけることを確認

### 4E-2 への申し送り

`DemandForecaster` インターフェースに **`predictedAt` が無い**ため、
現在のルールベース実装は「今日時点」でしか予測できず、**バックテストができない**。
4E-2 で新しい予測器を作るときは、最初から `predictedAt` を受け取る設計にすること。
過去日を指定して当時の情報だけで予測できれば、実データ1年分で
モデル選択（Ridge vs GBM）の判断ができるようになる。

---

## 15. 4E-2 の実装結果（2026/8/11）

### 実データでの決着：Ridge が GBM に勝った

| アルゴリズム | 検証MAE | 採用 |
|---|---|---|
| **Ridge** | **3.08pt** | ● |
| GBM | 5.57pt | |

学習サンプル1,047件、学習時間はHTTP往復込みで1.7秒。
§2 で「データが数百行しかないので勾配ブースティングの実力は出ない」と予測したとおりの結果で、
**Python サイドカーを立てない判断が実測で裏付けられた**。

ただしこの数字はまだ本番精度の主張ではない。実データは2025年1〜2月の119日分で、
残りはseedが混ざっている。実運用1年分が貯まった時点で測り直し、
§2 の移行基準（MAE±10ptを超え続けるならPythonへ）に照らして判断する。

### モデルが学習した内容（妥当性の確認）

寄与度の上位は業務感覚と一致した:

| 順位 | 特徴量 | 寄与度 |
|---|---|---|
| 1 | 季節性（cos） | 0.253 |
| 2 | 季節性（sin） | 0.122 |
| 3 | 週末 | 0.088 |
| 4 | オンハンド稼働率 | 0.069 |
| 5 | 同曜日移動平均28日 | 0.066 |

季節 → 週末 → 予約の積み上がり → 直近の同曜日実績、という順序は
レベニューマネージャーの判断順とほぼ同じで、モデルが変な相関を掴んでいないことの傍証になる。

### 学習サンプルを11倍に増やす工夫

「1日 = 1サンプル」だと1年で365行しか集まらない。そこで**同じ宿泊日を
複数のリードタイム（1/3/7/14/21/30/45/60/90/120/180日前）に展開**した。

これは単なる水増しではない。リードタイムは特徴量なので、モデルは
「近い日付ではオンハンドを信じ、遠い日付では季節性に頼る」という
予測地平ごとの振る舞いを学習できる。実際、確信度は予測地平とともに
0.844（近い）→ 0.512（遠い）と減衰することを確認した。

### 作ったもの

| ファイル | 役割 |
|---|---|
| `models/ridge.ts` | リッジ回帰。正規方程式をガウス消去で解く。λは検証誤差で選ぶ |
| `models/gbm.ts` | 勾配ブースティング回帰木。浅い木・小さい学習率・早期終了で過学習を抑える |
| `trainingService.ts` | サンプル生成 → 時系列分割 → 両モデル学習 → 検証誤差で採用 → DB保存 |
| `learnedForecaster.ts` | 保存済みモデルで予測。学習済みが無ければ null（ルールベースへ倒す） |
| `ForecastModel` モデル | 学習済みモデルの保存。採用しなかった候補の誤差も残す |
| `POST /api/v1/pricing/train` | 学習の実行（MANAGER以上・監査対象） |

### 設計判断

- **外部ライブラリを入れなかった**: 特徴量25個・サンプル数千では、
  ガウス消去と回帰木の自前実装で十分に速い（学習1.7秒）。
  依存を増やすとクラウド選定とデプロイの制約が増える
- **採用しなかった候補の誤差も保存する**: 「なぜRidgeが選ばれたか」を
  後から説明できないと、モデル選択がブラックボックスになる
- **学習済みモデルが無ければ黙ってルールベースへ倒す**: 導入直後や
  データ不足の施設で、画面が空になるのを避ける
- **予測器は価格を入力に取らない**（設計 §3）: 価格→需要→価格の循環を作らない

### 4E-3 への申し送り

- 確信度は現在 `horizonDecay × modelFit` の2因子のみ。
  残る `dataSufficiency` と `stability`（異常値検知）は、予約推移の逸脱判定と
  合わせて 4E-3 で算出する。暫定値を作り込むと後から意味が変わるため、
  根拠のある2因子だけで動かしている
- 推奨ランクの決定は暫定実装（稼働率をランクの並びに素朴に写しているだけ）。
  在庫・競合・特日を織り込んだ本来のロジックは 4E-3 の価格決定エージェントで作る
